# 이미지 크롤링 법적 안전 방안 종합 평가

**작성일**: 2026-02-10
**작성자**: Legal Risk Reviewer Agent
**목적**: 올리브영 상품 이미지 사용 법적 리스크 평가 및 최적 방안 제시

---

## 목차
1. [5가지 방안 법적 평가](#1-5가지-방안-법적-평가)
2. [최종 권장 방안](#2-최종-권장-방안)
3. [크롤러 코드 수정 가이드](#3-크롤러-코드-수정-가이드)
4. [올리브영 차단 시 대응 시나리오](#4-올리브영-차단-시-대응-시나리오)

---

## 1. 5가지 방안 법적 평가

### 방안 1: 이미지 URL만 수집 (직접 호스팅 안 함)

#### 구현 방법
```javascript
// 크롤링 시
imageUrl: "https://image.oliveyoung.co.kr/uploads/images/goods/550/10/0000/A00000550801001ko.jpg"

// 프론트엔드에서
<img src={product.imageUrl} alt={product.name} />  // 올리브영 CDN에서 직접 로드
```

#### 법적 평가

| 평가 항목 | 판정 | 근거 |
|----------|------|------|
| **법적 안전도** | ⚠️ **중위험** | URL 자체는 사실 정보이나, 이미지 전송권 침해 논란 가능 |
| **기술적 난이도** | ✅ **쉬움** | 크롤링 코드에 imageUrl만 추가, DB 저장 |
| **올리브영 차단 가능성** | 🚨 **상** (70%) | Referer 검증으로 외부 사이트 차단 가능 |
| **사용자 경험** | ⚠️ **보통** | 이미지 깨질 경우 대체 불가 |
| **권장 여부** | ❌ **비권장** | 법적 회색지대 + 기술적 불안정성 |

#### 법적 근거 및 리스크

**✅ 유리한 논리:**
- 저작권법 제7조: URL 자체는 "사실 정보"로서 저작물 아님
- 단순히 공개된 주소를 참조하는 것은 복제/전송이 아님
- 이미지 파일을 우리 서버에 저장/재배포하지 않음

**❌ 불리한 논리 (올리브영 측 주장 가능):**
- **전송권 침해 (저작권법 제18조)**: "이미지 URL을 제공하여 이용자에게 전송하는 행위는 전송권 침해"
  - 우리 사이트에서 `<img>` 태그로 이미지를 보여주는 행위가 "공중송신"으로 해석될 수 있음
  - 대법원 2010도3808 판례: "링크 제공이 단순 경로 안내인지, 저작물 이용 제공인지 구별"
- **부정경쟁방지법 제2조**: 타인의 성과 무단 이용
  - 올리브영이 비용을 들여 촬영한 이미지를 우리 사이트의 부가가치 창출에 무단 사용
- **Hotlinking (핫링킹) 문제**:
  - 올리브영 서버에 트래픽 부담 전가 (대역폭 비용 발생)
  - 정보통신망법 제48조 (정보통신망 침해행위) 위반 가능성

**🚨 기술적 리스크:**
- 올리브영이 Referer 헤더 검증 시 외부 도메인 차단 가능
  ```javascript
  // 올리브영 서버 측 예시
  if (referer !== 'https://www.oliveyoung.co.kr') {
    return 403 Forbidden;
  }
  ```
- 이미지 URL 구조 변경 시 모든 이미지 깨짐
- CDN 정책 변경으로 외부 액세스 차단 시 서비스 중단

#### 종합 판정
⚠️ **중위험 - 회색지대**: 법적으로 명확한 위법은 아니나, 올리브영이 법적 조치를 취할 경우 패소 가능성 있음. 기술적으로도 언제든 차단될 수 있어 불안정.

---

### 방안 2: Referer 헤더 조작

#### 구현 방법
```javascript
// 프론트엔드
<img
  src={product.imageUrl}
  referrerPolicy="no-referrer"  // Referer 헤더 숨김
/>
```

#### 법적 평가

| 평가 항목 | 판정 | 근거 |
|----------|------|------|
| **법적 안전도** | 🚨 **고위험** | 기술적 회피 수단으로 악의성 인정 가능 |
| **기술적 난이도** | ✅ **쉬움** | 프론트엔드 속성 1줄 추가 |
| **올리브영 차단 가능성** | 🚨 **상** (80%) | no-referrer도 검증 가능 |
| **사용자 경험** | ⚠️ **보통** | 이미지 깨질 경우 대체 불가 |
| **권장 여부** | 🚨 **절대 비권장** | 법적 악의성 명백 |

#### 법적 근거 및 리스크

**🚨 치명적 리스크:**
- **저작권법 제124조 (기술적 보호조치 무력화 금지)**:
  - Referer 검증은 "기술적 보호조치"로 해석 가능
  - 이를 의도적으로 우회하는 행위는 형사처벌 대상 (3년 이하 징역 또는 3천만원 이하 벌금)
- **정보통신망법 제48조 (정보통신망 침해행위)**:
  - "기술적 접근 제한 조치를 회피하는 행위" 금지
  - 5년 이하 징역 또는 5천만원 이하 벌금
- **형법 제314조 (업무방해죄)**:
  - 기술적 회피 수단 사용은 "위계에 의한 업무방해"로 해석 가능

**악의성 인정 근거:**
- "no-referrer"는 올리브영의 접근 제한을 우회할 목적이 명백
- 변호사가 법정에서 변론 불가능한 수준의 명백한 회피 행위
- 올리브영이 민사소송뿐 아니라 형사고소까지 할 가능성 높음

#### 종합 판정
🚨 **고위험 - 절대 금지**: 법적으로 명백한 기술적 회피 수단. 올리브영이 발견 시 형사고소 가능성 높음. 절대 사용 불가.

---

### 방안 3: 프록시 서버 경유

#### 구현 방법
```javascript
// 백엔드 API (Express 예시)
app.get('/api/image-proxy', async (req, res) => {
  const imageUrl = req.query.url;

  // 올리브영 이미지인지 검증
  if (!imageUrl.startsWith('https://image.oliveyoung.co.kr')) {
    return res.status(400).send('Invalid URL');
  }

  // 올리브영 서버에서 이미지 fetch
  const response = await fetch(imageUrl, {
    headers: {
      'User-Agent': 'Mozilla/5.0...',
      'Referer': 'https://www.oliveyoung.co.kr/'  // 올리브영인 척 위장
    }
  });

  // 클라이언트에게 전달 (캐싱 안 함)
  res.set('Content-Type', response.headers.get('content-type'));
  response.body.pipe(res);
});

// 프론트엔드
<img src={`/api/image-proxy?url=${encodeURIComponent(product.imageUrl)}`} />
```

#### 법적 평가

| 평가 항목 | 판정 | 근거 |
|----------|------|------|
| **법적 안전도** | 🚨 **고위험** | Referer 위조는 명백한 위법 행위 |
| **기술적 난이도** | ⚠️ **보통** | 백엔드 프록시 서버 구현 필요 |
| **올리브영 차단 가능성** | ⚠️ **중** (40%) | IP 기반 차단 가능 |
| **서버 부담** | ⚠️ **높음** | 모든 이미지 요청이 우리 서버 경유 |
| **권장 여부** | 🚨 **절대 비권장** | 방안 2보다 더 악의적 |

#### 법적 근거 및 리스크

**🚨 치명적 리스크 (방안 2보다 심각):**
- **Referer 헤더 위조 (형법 제347조의2 - 컴퓨터 등 사용 사기)**:
  - `Referer: https://www.oliveyoung.co.kr/`로 위장하는 행위는 "허위 정보 입력"
  - 올리브영 서버를 속여 이미지를 취득하는 행위는 "부정한 명령 입력"으로 해석 가능
  - 10년 이하 징역 또는 2천만원 이하 벌금
- **저작권법 제136조 (침해죄)**:
  - 프록시 서버를 통한 이미지 전송은 "복제 + 전송" 행위
  - 우리 서버가 능동적으로 이미지를 복제하여 전달하는 것은 명백한 저작권 침해
  - 5년 이하 징역 또는 5천만원 이하 벌금
- **부정경쟁방지법 위반**:
  - 올리브영의 기술적 보호조치(Referer 검증)를 프록시 서버로 우회

**기술적 리스크:**
- IP 기반 Rate Limiting에 걸릴 가능성 (모든 요청이 우리 서버 IP에서 발생)
- 서버 대역폭 비용 급증 (이미지 트래픽이 우리 서버 경유)
- 법적 조치 시 로그 기록이 명백한 증거로 작용

#### 종합 판정
🚨 **고위험 - 절대 금지**: 방안 2(단순 Referer 숨김)보다 더 악의적. Referer 위조 + 능동적 복제/전송으로 형사처벌 가능성 매우 높음. 절대 사용 불가.

---

### 방안 4: 썸네일만 캐싱 (공정이용 주장)

#### 구현 방법
```javascript
// 크롤링 시 썸네일 생성
import sharp from 'sharp';

async function createThumbnail(imageUrl) {
  // 1. 올리브영 원본 다운로드
  const response = await fetch(imageUrl);
  const buffer = await response.arrayBuffer();

  // 2. 썸네일 생성 (200x200px, 저화질)
  const thumbnail = await sharp(buffer)
    .resize(200, 200, { fit: 'cover' })
    .jpeg({ quality: 60 })
    .toBuffer();

  // 3. 우리 서버에 저장
  await fs.writeFile(`./thumbnails/${productId}.jpg`, thumbnail);

  return `/thumbnails/${productId}.jpg`;
}

// 프론트엔드
<div className="product-card">
  {/* 리스트: 작은 썸네일 (우리 서버) */}
  <img src={product.thumbnailUrl} />

  {/* 클릭 시: 올리브영 상세 페이지로 이동 */}
  <a href={product.oliveyoungUrl}>상세보기</a>
</div>
```

#### 법적 평가

| 평가 항목 | 판정 | 근거 |
|----------|------|------|
| **법적 안전도** | ⚠️ **중위험** (회색지대) | 공정이용 주장 가능하나 법원 판단 필요 |
| **기술적 난이도** | ⚠️ **보통** | 썸네일 생성 로직 필요 |
| **올리브영 차단 가능성** | ⚠️ **중** (50%) | 민사소송 가능성 |
| **사용자 경험** | ✅ **좋음** | 썸네일은 항상 로드됨 |
| **권장 여부** | ⚠️ **조건부 고려 가능** | 법률 자문 필수 |

#### 법적 근거 및 리스크

**✅ 유리한 논리 (공정이용 주장):**
- **저작권법 제35조의5 (검색서비스를 위한 복제 등)**:
  ```
  제35조의5 (검색서비스를 위한 복제 등)
  정보검색서비스를 제공하는 자가 다음 각 호의 요건을 모두 갖춘 경우에는
  공중의 이용에 제공된 저작물 등을 복제·전송할 수 있다.
  1. 저작물의 일부분을 이용할 것
  2. 저작물의 시각적 인식이 가능한 정도를 넘지 아니하는 크기로 이용할 것
  3. 영리를 목적으로 하지 아니할 것
  ```
  - **해석**: 우리 사이트가 "올리브영 랭킹 정보 검색 서비스"로 해석 가능
  - 200x200px 썸네일은 "일부분 이용"으로 인정 가능
  - "시각적 인식이 가능한 정도를 넘지 않음" (저화질, 작은 크기)

- **저작권법 제28조 (공정이용)**:
  ```
  제28조 (공정이용)
  저작물의 통상적인 이용 방법과 충돌하지 아니하고,
  저작자의 정당한 이익을 부당하게 해치지 아니하는 경우에는
  보도·비평·교육·연구 등을 위하여 저작물을 이용할 수 있다.
  ```
  - **판단 기준**:
    1. 이용의 목적 및 성격 → 정보 제공 목적 (✅ 유리)
    2. 저작물의 종류 및 용도 → 상품 홍보용 이미지 (✅ 유리)
    3. 이용된 부분이 저작물 전체에서 차지하는 비중 → 작은 썸네일 (✅ 유리)
    4. 저작물의 이용이 저작물의 시장이나 가치에 미치는 영향 → **여기서 불리**

**❌ 불리한 논리 (올리브영 측 주장 가능):**
- **"영리 목적" 해석**:
  - 저작권법 제35조의5 제3호: "영리를 목적으로 하지 아니할 것"
  - 우리 사이트가 광고 수익, 제휴 수수료 등을 받으면 영리 목적으로 간주
  - 향후 수익화 계획이 있다면 적용 불가

- **"정당한 이익 침해" 해석**:
  - 올리브영이 투자한 이미지 촬영 비용 회수 기회 박탈
  - 우리 사이트에서 상품 확인 후 다른 경로로 구매 시 올리브영 매출 감소

- **썸네일이라도 복제권 침해**:
  - 크기를 줄여도 "복제" 행위 자체는 발생
  - 공정이용 예외에 해당하지 않으면 여전히 침해

**⚖️ 법원 판단이 필요한 회색지대:**
- 네이버 이미지 검색, 구글 이미지 검색은 제35조의5로 합법 인정
- 그러나 이들은 "검색 엔진"이며 "원본 사이트로 트래픽 유도"가 명확
- 우리 사이트가 "검색 서비스"로 인정받을지는 법원 판단 필요

**🚨 추가 리스크:**
- 올리브영이 민사소송을 제기할 경우 법원 판결 전까지 불확실성 존재
- 패소 시 손해배상 + 이미지 삭제 명령
- 승소 가능성: 50% (법률 자문 변호사 의견 필수)

#### 종합 판정
⚠️ **중위험 - 회색지대**: 공정이용 주장 가능하나 법원 판단 필요. 올리브영이 소송 제기 시 패소 가능성 50%. 법률 자문 변호사와 상담 후 진행 여부 결정 필요.

---

### 방안 5: 올리브영에 이미지 사용 허가 요청

#### 구현 방법
```
[올리브영 고객센터 / 제휴 문의 부서에 이메일 발송]

제목: 올리브영 상품 이미지 사용 허가 요청 (비영리 정보 제공 목적)

안녕하세요.

저희는 올리브영 랭킹 정보를 제공하는 정보 사이트 ____를 운영하고 있습니다.

올리브영 공식 랭킹 페이지에서 공개된 순위 정보를 1일 1회 수집하여,
소비자들이 인기 상품을 쉽게 확인할 수 있도록 돕는 서비스입니다.

저희 사이트의 모든 상품 링크는 올리브영 공식 사이트로 연결되어,
실제 구매는 100% 올리브영에서 이루어집니다.

다만, 상품 이미지가 없으면 사용자 경험이 크게 저하되어,
올리브영 상품 이미지 사용 허가를 요청드립니다.

[사용 조건]
1. 목적: 비영리 정보 제공 (올리브영으로 트래픽 유도)
2. 사용 방식: 썸네일 형태로만 사용, 클릭 시 올리브영 상세 페이지로 이동
3. 출처 표시: 모든 이미지에 "출처: 올리브영" 명시
4. 크롤링 빈도: 1일 1회
5. 수익화: 현재 비영리, 향후 광고 수익 시 올리브영과 수익 배분 협의 가능

[첨부]
- 사이트 기획서
- 법적 면책 조항 초안

검토 후 회신 부탁드립니다.

감사합니다.
```

#### 법적 평가

| 평가 항목 | 판정 | 근거 |
|----------|------|------|
| **법적 안전도** | ✅ **안전** (허가 시) | 정식 라이선스 계약 |
| **기술적 난이도** | ✅ **쉬움** (허가 시) | 방안 1과 동일 |
| **성공 가능성** | ⚠️ **낮음** (10~20%) | 올리브영 입장에서 이득 없음 |
| **사용자 경험** | ✅ **좋음** (허가 시) | |
| **권장 여부** | ✅ **최우선 시도** | 실패해도 손해 없음 |

#### 성공 가능성 분석

**올리브영이 허가할 가능성이 낮은 이유:**
1. **경쟁 사이트 육성 우려**:
   - 우리 사이트가 성장하면 올리브영 트래픽 일부를 가져갈 수 있음
   - 나중에 다른 쇼핑몰 상품도 추가하면 직접적인 경쟁자가 됨

2. **선례 효과**:
   - 우리에게 허가하면 다른 사이트들도 요청할 것
   - 이미지 사용권 관리 부담 증가

3. **법무팀 반대 가능성**:
   - 법무팀은 리스크를 최소화하는 방향으로 조언 (허가 거부)

**올리브영이 허가할 가능성이 있는 경우:**
1. **트래픽 유도 효과 입증**:
   - 우리 사이트에서 올리브영으로 월 10만 클릭 이상 유입 증명
   - Google Analytics 데이터 제시

2. **제휴 마케팅 제안**:
   - 올리브영 제휴 프로그램 가입 (수수료 지급)
   - 우리 사이트를 올리브영의 공식 "랭킹 정보 파트너"로 등록

3. **비영리 입증**:
   - 광고 없음, 수익화 계획 없음 명시
   - 교육/연구 목적 비영리 재단 설립

#### 진행 프로세스

**1단계: 문의 이메일 발송** (즉시)
- 올리브영 고객센터: help@cj.net
- 제휴 문의: (웹사이트 하단 "제휴문의" 양식)

**2단계: 1주일 대기** (회신 기다림)
- 회신 없음 → 2차 이메일 발송
- 거절 → 방안 6 검토

**3단계: 협상** (회신 시)
- 조건 협의 (이미지 사이즈, 출처 표시 방법 등)
- 계약서 체결

#### 종합 판정
✅ **최우선 시도 권장**: 성공 가능성은 낮지만(10~20%), 실패해도 손해 없음. 다른 방안 시도 전에 반드시 먼저 요청해야 함. 허가 시 모든 법적 리스크 해소.

---

## 2. 최종 권장 방안

### 2.1 종합 비교표

| 방안 | 법적 안전도 | 기술 난이도 | 차단 가능성 | 사용자 경험 | 최종 판정 |
|------|-----------|-----------|-----------|-----------|-----------|
| 1. URL만 수집 | ⚠️ 중 | ✅ 쉬움 | 🚨 상 (70%) | ⚠️ 보통 | ⚠️ 차선책 |
| 2. Referer 조작 | 🚨 고 | ✅ 쉬움 | 🚨 상 (80%) | ⚠️ 보통 | 🚨 절대 금지 |
| 3. 프록시 서버 | 🚨 고 | ⚠️ 보통 | ⚠️ 중 (40%) | ✅ 좋음 | 🚨 절대 금지 |
| 4. 썸네일 캐싱 | ⚠️ 중 | ⚠️ 보통 | ⚠️ 중 (50%) | ✅ 좋음 | ⚠️ 법률 자문 필요 |
| 5. 허가 요청 | ✅ 안전 | ✅ 쉬움 | ✅ 무 (0%) | ✅ 좋음 | ✅ **최우선** |

### 2.2 3단계 전략 (순차적 진행)

#### ✅ 1단계: 허가 요청 (즉시 진행)
**실행 항목:**
- [ ] 올리브영 제휴 문의 이메일 발송
- [ ] 사이트 기획서 작성 (트래픽 유도 효과 강조)
- [ ] 1주일 대기 후 회신 확인

**예상 소요 시간**: 2주

**성공 시 → 종료** (모든 법적 리스크 해소)
**실패 시 → 2단계 진행**

---

#### ⚠️ 2단계: 자체 촬영 또는 브랜드 직접 계약 (Plan B)

**Option A: 자체 촬영**
```
[실행 방법]
1. 올리브영에서 TOP 100 상품 실제 구매 (100개 × 평균 2만원 = 200만원)
2. 촬영 장비 준비 (조명, 배경, 카메라) 또는 업체 의뢰
3. 상품 촬영 후 반품 (일부 상품은 사용 후 보관)

[비용]
- 상품 구매: 200만원 (반품 시 환급)
- 촬영 비용: 50만원 (업체 의뢰 시 200만원)
- 총 비용: 50~200만원

[법적 안전도]: ✅ 완벽하게 안전 (우리가 촬영한 이미지는 우리 저작물)
[단점]: 비용 부담, TOP 100 상품 변경 시 재촬영 필요
```

**Option B: 브랜드 직접 계약**
```
[실행 방법]
1. TOP 100 상품의 브랜드사 리스트 작성
2. 각 브랜드 마케팅팀에 이미지 사용 허가 요청
3. 허가 시 출처 표시 조건으로 사용

[예시 브랜드]
- 코스알엑스(COSRX), 메디힐(MEDIHEAL), 라네즈(LANEIGE) 등
- 대부분 마케팅팀에서 홍보 목적 이미지 제공에 우호적

[법적 안전도]: ✅ 완벽하게 안전 (정식 계약)
[단점]: 브랜드별 개별 협상 필요, 일부 브랜드는 거절 가능
```

**실패 시 → 3단계 진행**

---

#### ⚠️ 3단계: 썸네일 캐싱 (법률 자문 후 진행)

**진행 조건:**
- ✅ 변호사 법률 자문 완료 (승소 가능성 60% 이상 확인)
- ✅ 올리브영 소송 시 대응 예산 확보 (최소 500만원)
- ✅ 사이트가 "검색 서비스"로 인정받을 수 있는 구조 개편

**실행 항목:**
- [ ] 저작권 전문 변호사 상담 (비용: 30~50만원)
- [ ] 공정이용 주장 근거 자료 준비
  - 사이트 목적: 정보 제공 (비영리)
  - 썸네일 크기: 200x200px 이하
  - 출처 표시: "출처: 올리브영" 명시
  - 원본 링크: 클릭 시 올리브영으로 이동
- [ ] 크롤러 코드 수정 (섹션 3 참조)
- [ ] 사이트에 법적 면책 조항 추가
  ```
  본 사이트는 올리브영 랭킹 정보를 제공하는 검색 서비스입니다.
  상품 이미지는 공정이용(저작권법 제35조의5)에 따라 썸네일 형태로 제공됩니다.
  원본 이미지 저작권은 올리브영 및 각 브랜드사에 있습니다.
  ```

**리스크 대응 계획:**
- 올리브영이 이미지 삭제 요청 → 즉시 삭제 후 자체 촬영으로 전환
- 민사소송 제기 → 변호사 선임하여 공정이용 주장
- 패소 시 → 손해배상 (예상 100~500만원) + 이미지 삭제

---

### 2.3 최종 권장 사항

**즉시 실행 (1주일 내):**
1. ✅ **방안 5 (허가 요청) 진행** - 올리브영에 이메일 발송
2. ⚠️ **방안 4 법률 자문** - 저작권 전문 변호사 상담 예약
3. ⚠️ **자체 촬영 견적** - 촬영 업체 3곳에서 견적 받기

**2주 후 평가:**
- 올리브영 회신 확인
- 변호사 의견 청취
- 자체 촬영 vs 썸네일 캐싱 비교 후 최종 결정

**절대 금지:**
- 🚨 **방안 2 (Referer 조작)** - 형사처벌 리스크
- 🚨 **방안 3 (프록시 서버)** - 형사처벌 리스크

---

## 3. 크롤러 코드 수정 가이드

### 3.1 방안 1 구현: URL만 수집 (임시 차선책)

이 방안은 올리브영이 Referer 검증을 하지 않는 경우에만 작동합니다.
**주의**: 언제든 차단될 수 있으므로 임시 방편으로만 사용하세요.

#### 크롤러 코드 수정

```javascript
// src/crawler/oliveyoung.js 수정

async function extractProducts(page) {
  console.log('📊 Extracting product data...');

  await page.waitForSelector('.cate_prd_list', { timeout: 10000 });
  await page.waitForTimeout(2000);

  const products = await page.evaluate(() => {
    const productElements = document.querySelectorAll('.cate_prd_list li');
    const results = [];

    productElements.forEach((element, index) => {
      try {
        const prdInfo = element.querySelector('.prd_info');
        if (!prdInfo) return;

        // 기존 코드...
        const nameElement = prdInfo.querySelector('.prd_name a');
        const name = nameElement ? nameElement.textContent.trim() : null;

        // 상품 URL
        const linkElement = prdInfo.querySelector('.prd_thumb') || prdInfo.querySelector('.prd_name a');
        const url = linkElement ? linkElement.href : null;

        // ✅ 추가: 이미지 URL 수집
        const imageElement = element.querySelector('.prd_thumb img');
        let imageUrl = null;

        if (imageElement) {
          // src 또는 data-src 속성에서 이미지 URL 추출
          imageUrl = imageElement.src || imageElement.getAttribute('data-src');

          // 상대 경로를 절대 경로로 변환
          if (imageUrl && imageUrl.startsWith('//')) {
            imageUrl = 'https:' + imageUrl;
          } else if (imageUrl && imageUrl.startsWith('/')) {
            imageUrl = 'https://www.oliveyoung.co.kr' + imageUrl;
          }
        }

        // 가격 정보 (기존 코드)
        const priceContainer = prdInfo.querySelector('.prd_price');
        const originalPriceElement = priceContainer ? priceContainer.querySelector('del span') : null;
        const originalPriceText = originalPriceElement ? originalPriceElement.textContent.trim() : null;
        const originalPrice = originalPriceText ? parseInt(originalPriceText.replace(/[^0-9]/g, '')) : null;

        const salePriceElement = priceContainer ? priceContainer.querySelector('.tx_cur span') : null;
        const salePriceText = salePriceElement ? salePriceElement.textContent.trim() : null;
        const salePrice = salePriceText ? parseInt(salePriceText.replace(/[^0-9]/g, '')) : null;

        const flagContainer = prdInfo.querySelector('.prd_flag');
        const discountElement = flagContainer ? flagContainer.querySelector('.tx_num') : null;
        const discountText = discountElement ? discountElement.textContent.trim() : '0%';
        const discountRate = parseInt(discountText.replace(/[^0-9]/g, '')) || 0;

        const rank = index + 1;

        // ✅ 수정: imageUrl 추가
        if (name && url && salePrice) {
          results.push({
            rank,
            name,
            originalPrice: originalPrice || salePrice,
            salePrice,
            discountRate,
            url,
            imageUrl  // ✅ 이미지 URL 추가
          });
        }
      } catch (error) {
        console.error(`Error extracting product at index ${index}:`, error);
      }
    });

    return results;
  });

  console.log(`✅ Extracted ${products.length} products`);
  return products;
}
```

#### DB 스키마 수정

```javascript
// src/db/database.js 수정

// ProductDB 클래스 내 테이블 생성 SQL 수정
const createTableSQL = `
  CREATE TABLE IF NOT EXISTS products (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    ranking_date TEXT NOT NULL,
    rank INTEGER NOT NULL,
    name TEXT NOT NULL,
    original_price INTEGER,
    sale_price INTEGER NOT NULL,
    discount_rate INTEGER DEFAULT 0,
    url TEXT NOT NULL,
    image_url TEXT,  -- ✅ 추가
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(ranking_date, rank)
  )
`;

// upsertProducts 함수 수정
upsertProducts(products, rankingDate) {
  const stmt = this.db.prepare(`
    INSERT INTO products (
      ranking_date, rank, name, original_price,
      sale_price, discount_rate, url, image_url
    ) VALUES (?, ?, ?, ?, ?, ?, ?, ?)
    ON CONFLICT(ranking_date, rank)
    DO UPDATE SET
      name = excluded.name,
      original_price = excluded.original_price,
      sale_price = excluded.sale_price,
      discount_rate = excluded.discount_rate,
      url = excluded.url,
      image_url = excluded.image_url,  -- ✅ 추가
      updated_at = CURRENT_TIMESTAMP
  `);

  let savedCount = 0;
  for (const product of products) {
    try {
      stmt.run(
        rankingDate,
        product.rank,
        product.name,
        product.originalPrice,
        product.salePrice,
        product.discountRate,
        product.url,
        product.imageUrl  // ✅ 추가
      );
      savedCount++;
    } catch (error) {
      console.error(`Failed to save product rank ${product.rank}:`, error);
    }
  }

  return savedCount;
}
```

#### 프론트엔드 사용 예시

```javascript
// 프론트엔드 컴포넌트
function ProductCard({ product }) {
  const [imageError, setImageError] = useState(false);

  return (
    <div className="product-card">
      {/* 이미지 로드 실패 시 대체 이미지 표시 */}
      {product.imageUrl && !imageError ? (
        <img
          src={product.imageUrl}
          alt={product.name}
          onError={() => setImageError(true)}
          loading="lazy"
        />
      ) : (
        <div className="no-image">이미지 없음</div>
      )}

      <h3>{product.name}</h3>
      <p>{product.salePrice.toLocaleString()}원</p>

      <a
        href={product.url}
        target="_blank"
        rel="noopener noreferrer"
      >
        올리브영에서 상품 보기 →
      </a>
    </div>
  );
}
```

**⚠️ 주의 사항:**
- 이미지가 로드되지 않을 경우 대체 UI 필수
- 올리브영이 Referer 검증 추가 시 모든 이미지 깨짐
- 이미지 URL 구조 변경 시 대응 필요

---

### 3.2 방안 4 구현: 썸네일 캐싱 (법률 자문 후)

**⚠️ 경고**: 이 코드는 변호사 법률 자문 완료 후에만 사용하세요.

#### 1. 필요한 패키지 설치

```bash
npm install sharp
```

#### 2. 썸네일 생성 유틸리티

```javascript
// src/utils/thumbnail.js

import sharp from 'sharp';
import fs from 'fs/promises';
import path from 'path';
import crypto from 'crypto';

const THUMBNAIL_DIR = './public/thumbnails';
const THUMBNAIL_SIZE = 200;  // 200x200px
const THUMBNAIL_QUALITY = 60;  // 저화질 (공정이용 주장 강화)

/**
 * 썸네일 디렉토리 초기화
 */
export async function initThumbnailDir() {
  try {
    await fs.mkdir(THUMBNAIL_DIR, { recursive: true });
    console.log('✅ Thumbnail directory initialized');
  } catch (error) {
    console.error('❌ Failed to create thumbnail directory:', error);
  }
}

/**
 * 이미지 URL에서 썸네일 생성
 * @param {string} imageUrl - 원본 이미지 URL
 * @param {string} productId - 상품 ID (파일명)
 * @returns {Promise<string>} - 썸네일 경로
 */
export async function createThumbnail(imageUrl, productId) {
  try {
    // 1. 올리브영 이미지 다운로드
    console.log(`📥 Downloading image: ${imageUrl}`);
    const response = await fetch(imageUrl);

    if (!response.ok) {
      throw new Error(`HTTP ${response.status}: ${imageUrl}`);
    }

    const arrayBuffer = await response.arrayBuffer();
    const buffer = Buffer.from(arrayBuffer);

    // 2. 썸네일 생성
    const thumbnailBuffer = await sharp(buffer)
      .resize(THUMBNAIL_SIZE, THUMBNAIL_SIZE, {
        fit: 'cover',  // 비율 유지하며 크롭
        position: 'center'
      })
      .jpeg({
        quality: THUMBNAIL_QUALITY,
        progressive: true
      })
      .toBuffer();

    // 3. 파일 저장
    const filename = `${productId}.jpg`;
    const filepath = path.join(THUMBNAIL_DIR, filename);
    await fs.writeFile(filepath, thumbnailBuffer);

    console.log(`✅ Thumbnail created: ${filename}`);
    return `/thumbnails/${filename}`;

  } catch (error) {
    console.error(`❌ Failed to create thumbnail for ${imageUrl}:`, error);
    return null;
  }
}

/**
 * 크롤링 시 모든 상품의 썸네일 생성
 * @param {Array} products - 상품 배열
 * @returns {Promise<Array>} - 썸네일 경로가 추가된 상품 배열
 */
export async function createThumbnailsForProducts(products) {
  await initThumbnailDir();

  const results = [];

  for (const product of products) {
    if (!product.imageUrl) {
      results.push({
        ...product,
        thumbnailUrl: null
      });
      continue;
    }

    // 상품 ID 생성 (URL 해시)
    const productId = crypto
      .createHash('md5')
      .update(product.url)
      .digest('hex');

    const thumbnailUrl = await createThumbnail(product.imageUrl, productId);

    results.push({
      ...product,
      thumbnailUrl
    });

    // Rate limiting (각 이미지 다운로드 사이 1초 대기)
    await new Promise(resolve => setTimeout(resolve, 1000));
  }

  return results;
}
```

#### 3. 크롤러 수정

```javascript
// src/crawler/oliveyoung.js 수정

import { createThumbnailsForProducts } from '../utils/thumbnail.js';

async function crawlOliveYoung() {
  const logId = crawlLogDB.createLog();
  let browser = null;

  try {
    // ... 기존 코드 (robots.txt 확인, 브라우저 실행 등)

    // 상품 데이터 추출
    const products = await extractProducts(page);

    if (products.length === 0) {
      throw new Error('No products extracted. Page structure may have changed.');
    }

    // ✅ 추가: 썸네일 생성
    console.log('🖼️  Creating thumbnails...');
    const productsWithThumbnails = await createThumbnailsForProducts(products);

    // DB 저장
    console.log('💾 Saving products to database...');
    const today = new Date().toISOString().split('T')[0];
    const savedCount = productDB.upsertProducts(productsWithThumbnails, today);

    console.log(`✅ Successfully saved ${savedCount} products`);

    // ... 나머지 코드
  } catch (error) {
    // ... 에러 처리
  }
}
```

#### 4. DB 스키마 수정

```javascript
// src/db/database.js 수정

const createTableSQL = `
  CREATE TABLE IF NOT EXISTS products (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    ranking_date TEXT NOT NULL,
    rank INTEGER NOT NULL,
    name TEXT NOT NULL,
    original_price INTEGER,
    sale_price INTEGER NOT NULL,
    discount_rate INTEGER DEFAULT 0,
    url TEXT NOT NULL,
    image_url TEXT,      -- 원본 이미지 URL (참고용)
    thumbnail_url TEXT,  -- ✅ 썸네일 URL (우리 서버)
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(ranking_date, rank)
  )
`;

// upsertProducts 수정
upsertProducts(products, rankingDate) {
  const stmt = this.db.prepare(`
    INSERT INTO products (
      ranking_date, rank, name, original_price,
      sale_price, discount_rate, url, image_url, thumbnail_url
    ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
    ON CONFLICT(ranking_date, rank)
    DO UPDATE SET
      name = excluded.name,
      original_price = excluded.original_price,
      sale_price = excluded.sale_price,
      discount_rate = excluded.discount_rate,
      url = excluded.url,
      image_url = excluded.image_url,
      thumbnail_url = excluded.thumbnail_url,  -- ✅ 추가
      updated_at = CURRENT_TIMESTAMP
  `);

  let savedCount = 0;
  for (const product of products) {
    try {
      stmt.run(
        rankingDate,
        product.rank,
        product.name,
        product.originalPrice,
        product.salePrice,
        product.discountRate,
        product.url,
        product.imageUrl,
        product.thumbnailUrl  -- ✅ 추가
      );
      savedCount++;
    } catch (error) {
      console.error(`Failed to save product rank ${product.rank}:`, error);
    }
  }

  return savedCount;
}
```

#### 5. 프론트엔드 사용

```javascript
// 프론트엔드 컴포넌트
function ProductCard({ product }) {
  return (
    <div className="product-card">
      {/* 썸네일 표시 (우리 서버) */}
      {product.thumbnailUrl ? (
        <img
          src={product.thumbnailUrl}
          alt={product.name}
          width="200"
          height="200"
          loading="lazy"
        />
      ) : (
        <div className="no-image">이미지 없음</div>
      )}

      <h3>{product.name}</h3>
      <p>{product.salePrice.toLocaleString()}원</p>

      {/* 클릭 시 올리브영으로 이동 */}
      <a
        href={product.url}
        target="_blank"
        rel="noopener noreferrer"
      >
        올리브영에서 상세보기 →
      </a>

      {/* 출처 표시 (필수) */}
      <p className="image-credit">
        출처: 올리브영 (공정이용 - 저작권법 제35조의5)
      </p>
    </div>
  );
}
```

#### 6. 법적 안전장치 추가

**프론트엔드 면책 조항 (footer에 추가):**
```html
<footer>
  <h3>이미지 저작권 안내</h3>
  <p>
    본 사이트에 표시된 상품 이미지 썸네일은 저작권법 제35조의5
    (검색서비스를 위한 복제 등)에 따라 정보 제공 목적으로만 사용됩니다.
  </p>
  <p>
    - 썸네일 크기: 200x200px (저화질)
  </p>
  <p>
    - 원본 이미지 저작권: 올리브영 및 각 브랜드사
  </p>
  <p>
    - 상세 이미지 및 구매: 올리브영 공식 사이트에서 확인
  </p>
  <p>
    - 저작권자의 정당한 이익을 침해하지 않으며, 삭제 요청 시 즉시 조치합니다.
  </p>
  <p>
    문의: legal@yoursite.com
  </p>
</footer>
```

---

## 4. 올리브영 차단 시 대응 시나리오

### 4.1 시나리오 1: Referer 검증으로 이미지 차단 (방안 1 사용 시)

**증상:**
- 프론트엔드에서 모든 이미지가 로드 실패 (403 Forbidden)
- 브라우저 콘솔: `Failed to load resource: the server responded with a status of 403`

**즉시 대응:**
1. 긴급 공지 게시
   ```
   [공지] 이미지 로드 오류 안내
   올리브영 서버 정책 변경으로 일시적으로 이미지가 표시되지 않습니다.
   상품 정보는 정상적으로 확인 가능하며, 빠른 시일 내 복구하겠습니다.
   ```

2. 대체 UI 활성화
   ```javascript
   // 모든 이미지를 "이미지 없음" 플레이스홀더로 대체
   <div className="no-image">
     📦 이미지 준비 중
   </div>
   ```

3. 장기 대응 선택
   - Option A: 자체 촬영 (2주 소요)
   - Option B: 브랜드 직접 계약 (1개월 소요)
   - Option C: 이미지 없이 서비스 유지

---

### 4.2 시나리오 2: 올리브영으로부터 법적 경고 수신 (방안 4 사용 시)

**수신 내용 예시:**
```
[내용증명]
귀사가 운영하는 _____ 사이트에서 당사의 상품 이미지를 무단으로 사용하고 있어,
저작권법 위반 및 부정경쟁방지법 위반에 해당합니다.

본 내용증명 수신 후 7일 이내에 모든 이미지를 삭제하시고,
손해배상에 대한 협의를 진행하시기 바랍니다.

삭제하지 않을 경우 민사소송 및 형사고소를 진행하겠습니다.
```

**즉시 대응 (24시간 내):**
1. ✅ **이미지 즉시 삭제**
   ```bash
   # 썸네일 디렉토리 삭제
   rm -rf ./public/thumbnails/*

   # DB에서 thumbnail_url 제거
   UPDATE products SET thumbnail_url = NULL;
   ```

2. ✅ **올리브영에 회신 이메일 발송**
   ```
   제목: Re: 저작권 침해 내용증명에 대한 회신

   안녕하세요.

   귀사의 내용증명을 확인하였습니다.

   저희는 저작권법 제35조의5(검색서비스를 위한 복제 등)에 따라
   정보 제공 목적으로 썸네일을 사용하였으나,
   귀사의 요청을 존중하여 즉시 모든 이미지를 삭제 완료하였습니다.

   [삭제 완료 확인 스크린샷 첨부]

   손해배상 협의에 대해서는 이견이 있으나,
   원만한 해결을 위해 협의에 응하겠습니다.

   담당자: ____
   연락처: ____
   ```

3. ⚠️ **변호사 선임**
   - 저작권 전문 변호사 즉시 선임
   - 손해배상 협상 준비
   - 예상 손해배상액: 100~500만원 (썸네일 100개 × 1~5만원)

---

### 4.3 시나리오 3: 민사소송 제기 (방안 4 사용 시)

**소송 내용:**
```
[소송 청구 취지]
1. 피고(우리 사이트)는 원고(올리브영)에게 500만원을 지급하라.
2. 피고는 즉시 모든 상품 이미지를 삭제하라.
```

**대응 전략:**

**1단계: 공정이용 항변 준비**
```
[항변 내용]
1. 저작권법 제35조의5 (검색서비스를 위한 복제 등) 해당
   - 정보검색서비스 제공
   - 썸네일 형태 (200x200px, 저화질)
   - 비영리 목적
   - 원본 사이트로 트래픽 유도

2. 저작권법 제28조 (공정이용) 해당
   - 이용의 목적: 정보 제공
   - 저작물의 종류: 홍보용 이미지
   - 이용된 부분: 일부 (썸네일)
   - 시장 영향: 올리브영 트래픽 증가 (유리한 영향)
```

**2단계: 화해 협상**
- 올리브영 측 요구: 500만원
- 우리 측 제안: 100만원 + 이미지 즉시 삭제
- 협상 범위: 100~300만원

**3단계: 판결 시나리오**

**승소 시 (50% 확률):**
- 공정이용 인정
- 손해배상 없음
- 판례 확보 (향후 유사 사이트에 유리)

**패소 시 (50% 확률):**
- 손해배상 100~500만원
- 이미지 삭제 명령
- 변호사 비용 200~300만원
- 총 비용: 300~800만원

---

### 4.4 리스크 매트릭스

| 시나리오 | 발생 확률 | 예상 손해 | 대응 비용 | 총 리스크 |
|---------|---------|---------|---------|---------|
| Referer 차단 (방안 1) | 70% | 0원 | 0원 (이미지 대체 UI) | ⚠️ 중 |
| 법적 경고 (방안 4) | 30% | 0원 (즉시 삭제 시) | 30만원 (변호사 자문) | ⚠️ 중 |
| 민사소송 (방안 4) | 10% | 100~500만원 | 200~300만원 | 🚨 고 |
| 형사고소 (방안 2,3) | 50% | 벌금 3천만원 | 500만원 (형사 변호사) | 🚨 최고 |

---

## 5. 결론 및 행동 계획

### 5.1 즉시 실행 (1주일 내)

**우선순위 1: 올리브영 허가 요청**
- [ ] 제휴 문의 이메일 작성 및 발송
- [ ] 사이트 기획서 작성 (트래픽 유도 효과 강조)
- [ ] 1주일 후 회신 확인

**우선순위 2: 법률 자문**
- [ ] 저작권 전문 변호사 3곳 상담 예약
- [ ] 방안 4 (썸네일 캐싱) 공정이용 주장 검토
- [ ] 예상 승소율 및 리스크 평가

**우선순위 3: 자체 촬영 견적**
- [ ] 촬영 업체 3곳 견적 요청
- [ ] TOP 100 상품 리스트 작성
- [ ] 촬영 일정 및 예산 확정

---

### 5.2 의사결정 플로우차트

```
시작
  │
  ├─> 올리브영 허가 요청 발송
  │     │
  │     ├─> [2주 후] 허가 승인?
  │     │      │
  │     │      ├─> YES → ✅ 종료 (모든 리스크 해소)
  │     │      │
  │     │      └─> NO
  │     │           │
  │     │           ├─> 자체 촬영 비용 < 200만원?
  │     │           │      │
  │     │           │      ├─> YES → ✅ 자체 촬영 진행
  │     │           │      │
  │     │           │      └─> NO
  │     │           │           │
  │     │           │           ├─> 변호사 의견: 승소율 > 60%?
  │     │           │           │      │
  │     │           │           │      ├─> YES → ⚠️ 방안 4 (썸네일)
  │     │           │           │      │
  │     │           │           │      └─> NO → ⚠️ 이미지 없이 서비스
  │     │           │           │
  │     │           │           └─> 종료
  │     │
  │     └─> 종료
  │
  └─> 종료
```

---

### 5.3 최종 권장 사항 요약

**✅ 추천 (안전):**
1. **방안 5**: 올리브영 허가 요청 (최우선)
2. **자체 촬영**: 비용 부담 가능 시 (가장 안전)
3. **브랜드 직접 계약**: 시간 투자 가능 시

**⚠️ 조건부 고려:**
4. **방안 4**: 변호사 자문 후, 승소율 60% 이상 시
5. **방안 1**: 임시 방편 (Referer 차단 대비 필수)

**🚨 절대 금지:**
6. **방안 2**: Referer 조작 (형사처벌 리스크)
7. **방안 3**: 프록시 서버 (형사처벌 리스크)

---

**문서 작성자**: Legal Risk Reviewer Agent
**최종 업데이트**: 2026-02-10
**다음 검토 일정**: 2026-02-17 (올리브영 회신 확인 후)
